import React from "react";
import { Text, View } from "react-native";

type Props = {
  onPress: () => void;
};

const Home = ({}) => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
